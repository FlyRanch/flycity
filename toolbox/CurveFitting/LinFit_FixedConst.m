function [fitresult, gof] = LinFit_FixedConst(x, y,linfit_const)

%CREATEFIT(AMP_RATIO,FZ_NORM_CUT)
%  Create a fit.
%
%  Data for 'untitled fit 1' fit:
%      X Input : x
%      Y Output: y
%  Output:
%      fitresult : a fit object representing the fit.
%      gof : structure with goodness-of fit info.
%
%  See also FIT, CFIT, SFIT.

%  Auto-generated by MATLAB on 04-Mar-2015 08:38:58


%% Fit: 'linear fit with fixed constant'.
[xData, yData] = prepareCurveData( x, y );

% Set up fittype and options.
ft = fittype( 'poly1' );
opts = fitoptions( ft );
opts.Lower = [-Inf linfit_const];
opts.Upper = [Inf linfit_const];

% Fit model to data.
[fitresult, gof] = fit( xData, yData, ft, opts );

% % Plot fit with data.
% figure( 'Name', 'untitled fit 1' );
% plot( fitresult, xData, yData );
% % Label axes
% xlabel( 'x' );
% ylabel( 'y' );
% grid on

